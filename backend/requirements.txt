fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# EmailStr on the order form: a typo in an email address means the bakery
# cannot reach the customer about their cake.
email-validator==2.2.0
pydantic-settings==2.7.0
httpx==0.28.1
python-multipart==0.0.20

# The IANA timezone database. Windows has none, and python:*-slim images do not
# ship one either — without it ZoneInfo("Europe/Paris") raises, and every
# lead-time and rush calculation depends on that zone.
tzdata==2024.2

# dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.6
